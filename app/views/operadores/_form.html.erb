<%= form_with(model: operadore, local: true) do |form| %>
  <% if operadore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(operadore.errors.count, "error") %> prohibited this operadore from being saved:</h2>

      <ul>
      <% operadore.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :telefono %>
    <%= form.text_field :telefono %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
