<%= form_with(model: route, local: true) do |form| %>
  <% if route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :operator_id %>
    <%= form.number_field :operator_id %>
  </div>

  <div class="field">
    <%= form.label :suburb_id %>
    <%= form.number_field :suburb_id %>
  </div>

  <div class="field">
    <%= form.label :horary %>
    <%= form.text_field :horary %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
