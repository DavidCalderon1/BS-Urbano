<%= form_with(model: rutum, local: true) do |form| %>
  <% if rutum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rutum.errors.count, "error") %> prohibited this rutum from being saved:</h2>

      <ul>
      <% rutum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :operador_id %>
    <%= form.number_field :operador_id %>
  </div>

  <div class="field">
    <%= form.label :barrio_id %>
    <%= form.number_field :barrio_id %>
  </div>

  <div class="field">
    <%= form.label :horario %>
    <%= form.text_field :horario %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
