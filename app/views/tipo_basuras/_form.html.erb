<%= form_with(model: tipo_basura, local: true) do |form| %>
  <% if tipo_basura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_basura.errors.count, "error") %> prohibited this tipo_basura from being saved:</h2>

      <ul>
      <% tipo_basura.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :foto %>
    <%= form.text_field :foto %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
