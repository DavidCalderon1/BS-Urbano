<%= form_with(model: garbage_type, local: true) do |form| %>
  <% if garbage_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(garbage_type.errors.count, "error") %> prohibited this garbage_type from being saved:</h2>

      <ul>
      <% garbage_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :picture %>
    <%= form.text_field :picture %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
