json.extract! subject, :id, :user_id, :suburb_id, :address, :latitude, :longitude, :garbage_type_id, :picture, :detail, :state_id, :created_at, :updated_at
json.url subject_url(subject, format: :json)
