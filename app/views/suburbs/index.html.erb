<p id="notice"><%= notice %></p>

<h1>Suburbs</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Locality</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @suburbs.each do |suburb| %>
      <tr>
        <td><%= suburb.name %></td>
        <td><%= suburb.locality_id %></td>
        <td><%= suburb.latitude %></td>
        <td><%= suburb.longitude %></td>
        <td><%= link_to 'Show', suburb %></td>
        <td><%= link_to 'Edit', edit_suburb_path(suburb) %></td>
        <td><%= link_to 'Destroy', suburb, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Suburb', new_suburb_path %>
