<%= form_with(model: publicacione, local: true) do |form| %>
  <% if publicacione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(publicacione.errors.count, "error") %> prohibited this publicacione from being saved:</h2>

      <ul>
      <% publicacione.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :foto %>
    <%= form.text_field :foto %>
  </div>

  <div class="field">
    <%= form.label :estado_id %>
    <%= form.number_field :estado_id %>
  </div>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.number_field :usuario_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
