<% if user_signed_in? %>
  <!-- Navigation -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-bottom">
      <div class="container d-flex justify-content-center">
        <ul class=" ">
            
            <%= link_to(home_path, :class => "nav-link", :style => "display: inline-block;") do %>
              <li class="nav-item fas fa-home"></li>
            <% end %>

            <%= link_to(subjects_path, :class => "nav-link", :style => "display: inline-block;") do %>
              <li class="nav-item far fa-lightbulb"></li>
            <% end %>

            <%= link_to(notifications_path, :class => "nav-link", :style => "display: inline-block;") do %>
              <li class="nav-item far fa-bell"></li>
            <% end %>

          </ul>
      </div>
    </nav>
    
<% end %>