<%= form_with(model: sugerencium, local: true) do |form| %>
  <% if sugerencium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sugerencium.errors.count, "error") %> prohibited this sugerencium from being saved:</h2>

      <ul>
      <% sugerencium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :detalle %>
    <%= form.text_field :detalle %>
  </div>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.number_field :usuario_id %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.datetime_select :fecha %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
